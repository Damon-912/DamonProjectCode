import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const InterfaceLogs: React.FC = () => {
  return (
    <div style={{ padding: 16 }}>
      <Card>
        <Title level={4}>接口日志</Title>
        <p>功能开发中...</p>
      </Card>
    </div>
  );
};

export default InterfaceLogs;
